import RiskConfiguration from '@/components/risk-configuration/RiskConfiguration';

export const metadata = {
  title: 'Risk Configuration',
};

export default function Page() {
  return <RiskConfiguration />;
}
